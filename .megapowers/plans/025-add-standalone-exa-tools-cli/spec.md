## Goal
Bump package version to 3.0.0 for npm republish.

## Acceptance Criteria
1. `package.json` `version` field is `"3.0.0"`.
2. No other `package.json` fields are changed.
3. All existing tests pass.
